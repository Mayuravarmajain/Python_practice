def isprime(num):
    if num < 2:
        return False
    for i in range(1,int(num**0.5)+1):
        if num % i == 0:
            return False
        else:
            return True

num=int(input("Enter the number to check is prime or not"))
print(f"{num} is the prime : {isprime(num)}")
